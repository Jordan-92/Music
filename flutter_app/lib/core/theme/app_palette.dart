import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor = Color(0xFF171717);
  static const Color gradient1 = Color.fromRGBO(0, 30, 150, 1);
  static const Color gradient2 = Color.fromRGBO(0, 140, 145, 1);
  static const Color gradient3 = Color.fromRGBO(15, 200, 225, 1);
  static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}

// TODO: use the color used in the web_app
// to convert from HEX : https://encycolorpedia.fr/373737#google_vignette

class AppColors {
  static const Color gradient1 = Color(0xFF1E40AF);
  static const Color gradient2 = Color(0xFF2563EB);
  // static const Color gradient1 = Color(0xFF065F46); // spotify color
  // static const Color gradient2 = Color(0xFF22c55e); // spotify color
  static const Color background0 = Color(0xFF000000); // black
  static const Color background = Color(0xFF171717);
  static const Color primary = Color(0xFFFFFFFF); // white text
  static const Color secondary = Color(0xFFA3A3A3); // grey text 
  static const Color tertiary = Color(0xFF000000); // black text for blue & white buttons 
  static const Color fixedBrightened = Color(0xff373737); // brightened
  static const Color brightened = Color(0xFFF5F5F5); // brightened/transparency to brighten object
  static const Color buttonAndInput = Color(0xFF404040);
  static const Color player = Color(0xFF000000); // black
  static const Color modal = Color(0xFF262626);
  static const Color slider = Color(0xFF525252);
}